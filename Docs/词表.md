# RuleCore 词表（当前全部主谓宾）

> 这份表是**从源码 `Model/RuleVocabularyCatalog.cs` 抽出来的**，不是设计稿。
> 改词表之后要同步改这份文件。
> 生成时间：2026-09-19 · 属性 42 · 谓词 26 · 主体绑定 7 · 归约 3 · 量词 3 · 能力位 29

规则语言的骨架是三元组：

```
当 ⟨实体表达式⟩⟨谓语⟩⟨宾语⟩ ，… ， ⟨实体表达式⟩⟨操作⟩⟨宾语⟩ ，…
```

逗号之前全是**检测**，之后全是**操作**。三块词表的组合方式见 §6。

---

## 0. 一条硬规矩：身份不是规则里的词

**「是囚犯」「是机械族」这类判断不在词表里，而且不该在。**

理由是一条能证明的话：主体绑定已经穷尽了身份。绑成「囚犯」，那一轮里每个人的身份
就都是囚犯；点了名，那个人的身份就是定死的那一个。于是 `本主体.是囚犯`
**对任何一条规则都是恒真句**——它不提供信息，只占属性列表的位置
（曾经 21 条小人属性里有 7 条是这种）。

想写「囚犯做某事」，就把主体绑成囚犯。想知道「现在这个主体到底是什么」，
**编辑器直接写在他脸上**：

```
点名一个人
  阿珍              换一个
  阿珍 现在是：囚犯

或者，让一类人各做一遍
  自由殖民者   5 个
  囚犯         3 个
  奴隶         0 个——规则不会跑
  殖民机械族   2 个
```

这条规矩的**收益在属性列表上**：绑成机械族，列表里就没有饱食度、有着电量；
绑成殖民者，反过来。玩家一次都不用判断。

> 什么时候该把身份判断加回来：**当出现一个宽到需要再区分的绑定时**
> （比如「这张图上的所有人」）。现在没有那种绑定，所以现在它们只能是常数。

---

## 1. 实体：根（实体表达式的起点）

| 根 | 产出类型 | 说明 |
|---|---|---|
| **某个具体小人的名字** | 小人 | 本次求值的执行者。点了名就显示**他的名字**，不是"本主体" |
| `本图` | 地图 | 本次求值所在的地图。引擎按地图实例化，所以规则对每张地图各跑一遍 |
| `全部自由殖民者` / `全部囚犯` / `全部奴隶` / `全部殖民机械族` / `全部殖民地动物` / `全部访客` | **一组**小人 | **"一群"= 一个根 + 一个键**，成群方式来自主体绑定表。后面必须接归约 |
| `全部地图` | **一组**地图 | 后面必须接归约 |
| `当前元素` | 与所属集合的元素同类型 | **只在筛选器里出现** |

> **「一群」读不了属性。** `全部机械族.电量` 不是"每个机械族的电量"，
> 而是"这一组东西的电量"——它根本不是一个值，编辑器不会给你属性列表。
>
> **想让每个成员各判一次**，那是**主体绑定**的事：
> 主体绑成「殖民机械族」，规则就会对每个机械族各跑一遍，各看各的电量。
> 见 §6。

---

## 2. 实体：属性（路径上的一步读操作）

属性挂在某个类型的实体上；类型不对就不出现在可选列表里。
**"所需能力"那一列才是这个版本的关键**：它决定这条属性在这个主体身上出不出现。

### 地图（14）

| 属性 | 产出 | 单位 | 范围 | 取值域 | 所需能力 |
|---|---|---|---|---|---|
| `本图.室外温度` | 数值 | ℃ | -200~200 | | 是地图 |
| `本图.天气` | 名字 | | | 全部 `WeatherDef` | 是地图 |
| `本图.降雨强度` | 百分比 | % | 0~1 | | 是地图 |
| `本图.降雪强度` | 百分比 | % | 0~1 | | 是地图 |
| `本图.天空亮度` | 数值 | | 0~1 | 白天接近 1，夜里接近 0 | 是地图 |
| `本图.自由殖民者数` | 数值 | | 0~1000 | | 是地图 |
| `本图.囚犯数` | 数值 | | 0~1000 | | 是地图 |
| `本图.奴隶数` | 数值 | | 0~1000 | | 是地图 |
| `本图.机械族数` | 数值 | | 0~1000 | | 是地图 |
| `本图.动物数` | 数值 | | 0~1000 | | 是地图 |
| `本图.总人数` | 数值 | | 0~1000 | 含敌人与动物 | 是地图 |
| `本图.地图财富` | 数值 | | 0~1e7 | | 是地图 |
| `本图.当地时间` | 数值 | 点 | 0~23 | | 是地图 |
| `本图.是白天` | 布尔 | | 配「为真」用 | 6:00–18:00，**与天气/日蚀无关** | 是地图 |

> 身份的**数量**在地图上是有意义的（"囚犯只有两个"），所以它们留在这里。
> 而身份的**判断**没有意义（见 §0）。

### 小人（22）

| 属性 | 产出 | 单位 | 取值域 | 所需能力 |
|---|---|---|---|---|
| `本主体.血量` | 百分比 | % | | 是小人 |
| `本主体.心情` | 百分比 | % | | **心情需求** |
| `本主体.饱食度` | 百分比 | % | | **饱食需求** |
| `本主体.精力` | 百分比 | % | | **休息需求** |
| `本主体.电量` | 百分比 | % | 机械族的「饱食度」 | **电量需求** |
| `本主体.年龄` | 数值 | 岁 | | 有年龄 |
| `本主体.位置` | 坐标 | | | 是小人 |
| `本主体.所在位置温度` | 数值 | ℃ | 房间内读房间温度，户外读室外温度 | 是小人 |
| `本主体.所在房间` | 房间 | | 在户外**读不到**（不是"房间为零"） | 是小人 |
| `本主体.着装` | **一组**物品 | | 后面必须接归约或筛选 | **能穿衣服** |
| `本主体.手持武器` | 物品 | | 空手**读不到** | **能拿东西** |
| `本主体.床位` | 物品 | | 他认领的那张床；没床位**读不到**。接 `.交互格` 就是一格 | **有床位** |
| `本主体.倒地` | 布尔 | | 配「为真」用 | 是小人 |
| `本主体.被征召` | 布尔 | | 配「为真」用 | **能被征召** |
| `本主体.在露天` | 布尔 | | 配「为真」用。**只答"有没有屋顶"** | 是小人 |
| `本主体.活动区` | 名字 | | 现场取：你画过的活动区 +「不受限」 | 是小人 |
| `本主体.着装方案` | 名字 | | 现场取：你建过的着装方案 | **能穿衣服** |
| `本主体.食物限制` | 名字 | | 现场取：你建过的食物限制 | **饱食需求** |
| `本主体.药物政策` | 名字 | | 现场取：你建过的药物政策 | 是人形 |
| `本主体.医疗级别` | 名字 | | 无 / 不用药 / 草药或更差 / 普通或更差 / 最佳 | 是人形 |
| `本主体.敌对反应` | 名字 | | 无视 / 攻击 / 逃跑 | 是人形 |
| `本主体.当前作息` | 名字 | | 睡觉 / 工作 / 娱乐…**只读** | 是人形 |

> **最后 7 条就是管制界面那一列。** 它们是"玩家自己在管制界面点两下也能做"的东西，
> 所以全部是**玩家级**——不需要开发者权限，规则只是替玩家做得更及时。
>
> 两个设计后果值得写下来：
>
> 1. **活动区 / 各种方案是"现场取"的**，不是写死的枚举。它们是玩家在游戏里自己建的，
>    所以候选列表每次打开都从 `Current.Game` 里现读。同一份规则换存档就会指向别的编号，
>    读不到时编辑器写「（已不存在：xxx）」而不是假装它是空的。
> 2. **作息只读，不给写。** 作息是 24 格的一张表，不是单值——要改它得先说清改哪一格。
>    在没有"格子"这个概念之前，"改作息"是个说不清的操作。**宁可不给，也不给一个说不清的动词。**
>
> 顺便：`避难格` 已经**删掉**了。那是我们自造的概念（最近的、有屋顶、走得到的格子），
> 游戏里根本不存在，而且每 tick 要扫 61×61 格再逐个试寻路。换成 `床位.交互格` 之后
> 「去某个地方」有了诚实的来源——两个属性都是原版真有的。

### 房间（2）

| 属性 | 产出 | 单位 | 所需能力 |
|---|---|---|---|
| `房间.温度` | 数值 | ℃ | 是房间 |
| `房间.大小` | 数值（格数） | | 是房间 |

### 物品（4）

| 属性 | 产出 | 单位 | 取值域 | 所需能力 |
|---|---|---|---|---|
| `物品.耐久` | 百分比 | % | | 是物品 |
| `物品.当前耐久值` | 数值 | | 绝对耐久，不是比例 | 是物品 |
| `物品.物品类型` | 名字 | | 全部 `ThingDef` | 是物品 |
| `物品.交互格` | 格子 | | 别人站上去就能用它那一格 | 是物品 |

---

## 3. 实体：归约（把一组变回单值）

| 归约 | 接受 | 产出 |
|---|---|---|
| `.数量` | 一组东西 | 数值 |
| `.第一个` | 一组东西 | 单个（确定性地取第一个） |
| `.最近` | 一组东西 | 单个（离本主体最近的） |

**手上是一组东西时，归约是强制的**：不接归约，既不能往下读属性，也不能选谓语。

### 量词（`对每个元素各判一次`）

| 量词 | 接受 | 产出 | 空集合 |
|---|---|---|---|
| `全都满足（条件）` | 一组东西 + 一个条件 | 布尔 | **成立**（空真） |
| `有一个满足（条件）` | 一组东西 + 一个条件 | 布尔 | 不成立 |
| `一个都不满足（条件）` | 一组东西 + 一个条件 | 布尔 | **成立** |

归约是**选出一个**（几个 / 哪一个），量词是**问一句关于整组的话**（真 / 假）。
两者的区别在编辑器里就写在同一个段落里，挨着放。

例：`本主体.着装[是 帽子].全都满足（当前元素.耐久 大于 50%）` —— 每件帽子都完好。

> **空集合上「全都满足」成立是刻意的决定**，也是数学上的标准读法：
> 「我没有帽子」确实是「我的帽子都没问题」。
> 代价要记住：`全部囚犯 全都满足 …` 在**一个囚犯都没有**时也成立，
> 所以「一共几个」和「每个怎么样」是两件事，前者另外用 `.数量`。

> 还缺：`求和` / `最大` / `最小`。它们要把一个属性**映射**到集合上再归约（lift），
> 产出的是数不是布尔，形状和量词又不同。

### 筛选（`[…]`）

筛选器在当前元素上开一个条件。里面是一句普通的三元组，主语固定是 `当前元素`。

例：`本主体.着装[物品.耐久 小于 50%].数量 大于 0`

---

## 4. 谓语：检测（产出真/假）

| 谓语 | 主语要求 | 宾语 | 所需能力 | 特殊 |
|---|---|---|---|---|
| `大于` `小于` `不小于` `不大于` `等于` `不等于` | **数值** | 数值 | — | 电平 |
| `是` | **名字** | 名字（域来自主语属性） | — | |
| `为真` | **布尔** | 无 | — | |
| `恒真` | 任意 | 无 | — | 永远成立，写纯周期规则用 |
| `空闲` | 小人 | 无 | — | **不成立时安静让开**（不算规则失败） |
| `能动` | 小人 | 无 | — | **不成立时安静让开** |
| `收到信件` | 地图 | 名字（全部 `LetterDef`） | — | **边沿**；实现是按到达时间开窗口 |
| `处于` | 地图 | 名字（全部 `GameConditionDef`） | — | 读**持续状态**：日蚀 / 毒雾 / 寒潮 / 心灵低语 |

---

## 5. 谓语：操作（产出改变）

| 操作 | 主语要求 | 宾语 | 所需能力 | 权限 |
|---|---|---|---|---|
| `写日志` | 任意 | 文本 | — | 玩家级 |
| `前往` | 小人 | 格子 | — | 玩家级（Agentic：下一条 job 就撒手） |
| `脱下` | 小人 | 物品 | **能穿衣服** | 开发者级（原版没有"给清醒的人脱衣服"的 job） |
| `发信件` | 地图 | 名字（全部 `LetterDef`） | — | 玩家级（只通知，不改世界） |
| **`变成`** | 地图 | 名字（全部 `WeatherDef`） | — | **开发者级**：直接改天气 |
| **`触发事件`** | 地图 | 名字（全部 `IncidentDef`） | — | **开发者级**：走原版事件系统 |
| **`充电`** | 小人（**有电量的**） | 数值（%） | **电量需求** | **开发者级**：直接推电量 |

### 管制界面那一列：6 个玩家级操作

| 操作 | 主语要求 | 宾语 | 所需能力 |
|---|---|---|---|
| `改活动区` | 小人 | 名字（活动区 +「不受限」） | 是小人 |
| `改着装方案` | 小人 | 名字（现场取的方案） | **能穿衣服** |
| `改食物限制` | 小人 | 名字（现场取的方案） | **饱食需求** |
| `改药物政策` | 小人 | 名字（现场取的政策） | 是人形 |
| `改医疗级别` | 小人 | 名字（5 个等级） | 是人形 |
| `改敌对反应` | 小人 | 名字（3 个反应） | 是人形 |

这六个**全是玩家级**，理由是一条：它们改的东西玩家自己在管制界面就能改。
开发者级留给"原版根本不给你这个按钮"的操作（直接改天气、给清醒的人脱衣服、推电量）。

两个实现上的诚实之处：

- **`改活动区` 要求执行者在地图上。** 原版在没有地图时会静默跳过（什么都不做、
  不报错）。这里把它记成一次失败并说明原因——**静默成功是最坏的一种成功**。
- **宾语读不到时不假装它是空的。** 存档里的活动区被删了，编辑器显示
  「（已不存在：放牧区）」；一个活动区都没有时显示
  「现在没有可以选的——先去管制界面建一个」，而不是给一个空下拉框。

### 地图：唯一一个"不通过小人就能改变世界"的地方

这一组补的是一个真实的洞：**地图原来只有一个「写日志」**，玩家点开地图的谓语列表看到的是一行。

而地图恰恰是规则语言里唯一能直接改变世界的地方——天气、事件、游戏状态都不需要谁来执行。

`触发事件` 的两个设计决定：

1. **走原版的事件系统**（`StorytellerUtility.DefaultParmsNow` → `Worker.CanFireNow` → `TryExecute`），
   不自己拼效果。这样事件的时长、信件、难度限制、"最近刚发生过"、剧本禁用全都还在。
2. **原版说"现在不能发生"就安静让开**（记 `Rejected`，不记冷却）。
   硬塞会把玩家自己设的那些规则全部踩掉——那是"原版第一顺位"。

> **要起游戏状态为什么用「触发事件」而不是单开一个动词**：
> 原版那些状态本来就由事件造成（`IncidentWorker_MakeGameCondition`），
> 而**时长在 `IncidentDef.durationDays` 上，不在 `GameConditionDef` 上**。
> 自己塞一个 `GameCondition` 就只能猜时长——那是凭空的。
> 所以读用 `处于`、写用 `触发事件`，两边对称。

### 权限层级：Developer 就是"能直接改世界状态"

| 层级 | 含义 | 怎么拿到 |
|---|---|---|
| 玩家级 | 只能"让谁去做"，动作可失败交回原版 | 默认 |
| 开发者级 | 可直接改世界状态 | 开原版开发者模式 **或** 在模组设置里勾「允许开发者级操作」 |

一条规则的实际层级 = **它所有操作里的最高级**。所以只要有一个开发者级动作，整条规则就是开发者级。

**开关放在玩家手里而不是"跟着 dev mode 走"**：这类操作确实强，但"强"该由玩家自己决定，
不该由他有没有按开调试菜单决定——把功能藏在调试菜单后面，等于让他去猜"是不是我规则写错了"。

编辑器上会直接标出来：`开发者级` 或 **`开发者级 · 未授权`**，未授权时下面还有一句去哪儿开。
**不把它们从列表里删掉**，因为规则是**可以分享的数据**——别人可能在有授权的机器上打开它，
藏起来会让那条规则看起来像坏了。

---

## 6. 组合规则：什么能接什么

### 值类型必须一路对得上

```
根的类型  →  每个属性/归约/筛选  →  最终类型  →  谓语是否接受
```

| 最终类型 | 能用的检测 |
|---|---|
| 数值 | `大于` `小于` `不小于` `不大于` `等于` `不等于` |
| 名字 | `是` |
| 布尔 | `为真` |
| 单个小人 | `空闲` `能动` |
| 单个地图 | `收到信件` |
| **一组东西** | **一个都不能用 —— 先归约** |

### 宾语类型也必须对得上

| 谓语 | 宾语要什么 |
|---|---|
| 数值比较 | 数值 |
| `是` | 名字 |
| `写日志` | 文本 |
| `前往` | **格子**（不是"一个人"，也不是坐标字面量） |
| `脱下` | **物品** |

### 主体绑定：**绑定就是身份**

**绑定有两种基数，这不是细节，是两件不同的事。**

| 绑定 | 基数 | 说明 |
|---|---|---|
| 不绑定 | 整图一次 | `本主体` 读不到 → 任何读 `本主体` 的检测**永远不成立** |
| **点名一个人** | **整条规则只跑一遍** | 必须再点名**是哪一位**；句子里直接写他的名字 |
| `自由殖民者` | 每个成员各跑一遍 | 冷却与自触发环按人分开算 |
| `囚犯` | 每个成员各跑一遍 | |
| `奴隶` | 每个成员各跑一遍 | |
| `殖民机械族` | 每个成员各跑一遍 | 属性列表里**不会出现饱食度**，出现的是电量 |
| `殖民地动物` | 每个成员各跑一遍 | 不会出现理念、征召 |
| `访客` | 每个成员各跑一遍 | 有人接待、既非囚犯也非奴隶 |

> 「让所有人一起做某事」**不是一个子句能表达的**。三元组的主语永远是一个东西；
> 「每个人各做一遍」是**主体绑定**的活。

**群体绑定旁边的数字是这一轮收出了几个**：0 也要显示，因为
「绑定成囚犯而殖民地没有囚犯」正是最该被看见的状态——规则会安静地什么都不做，
而玩家只会以为自己条件写错了。

> **点名绑定的一个诚实的局限**：玩家规则住在**全局配置**里，而人的引用（`ThingID`）
> 是**每个存档各自发的**。所以「点名某人」的规则换一个存档就找不到那个人，
> 于是不触发。编辑器会把这件事说出来，而不是让规则静默地什么都不做。

### 第四维：宿主能力

类型过滤是**三维**的，加上能力位之后：

| 维度 | 决定 | 例子 |
|---|---|---|
| 实体类型 | 能不能作用在这个东西上 | `耐久` 不出现在地图上 |
| 值类型 | 这个谓语吃不吃得下它现在的值 | `大于` 不出现在小人身上（小人不是数） |
| **能力位** | **这个宿主身上这件事存不存在** | **`饱食度` 不出现在机械族身上** |

能力位共 29 个，分四组：

- **实体大类**：是小人 / 是地图 / 是物品 / 是房间 / 是格子
- **生理构成**：人形 / 机械族 / 动物 / 血肉之躯
- **需求**：心情 / 饱食 / 休息 / 电量 / 娱乐 / 舒适 / 美观
- **装备与身份与能力**：能穿衣服 / 能拿东西 / 有随身物品 / 能拥有床位 /
  殖民地成员 / 囚犯 / 奴隶 / 访客 / 敌对 / 能被征召 / 有技能 / 有理念 / 有年龄

编辑器拿**当前主体样本**去比（点名就是他本人；群体绑定取成员的并集），
缺哪一位就把那条从列表里拿掉，并在下方**说清缺的是什么**：

```
这些用不上（当前主体身上没有它们要的东西）
  饱食度  —— 缺：饱食需求
  着装    —— 缺：能穿衣服
```

**"没有它"和"你选错了主体"必须分开说**——玩家看到的是同一个短列表，
但该做的下一步完全不同。

---

## 7. 现在写得出 / 写不出

### 写得出

```
主体绑定：点名一个人 → 小明

当  小明.着装[物品.耐久 小于 50%].数量   大于   0
且  小明.心情                          小于   30%
且  本图.天空亮度                       小于   0.3
，  小明     前往   小明.床位.交互格
且  小明     脱下   小明.着装[物品.耐久 小于 50%].第一个
```

> `小明.床位.交互格` 是一条**两跳实体路径**：小人 → 他认领的床 → 站上去用那张床的格子。
> 之所以不用原来那个「避难格」，见 §2 小人那一节的脚注。

```
主体绑定：殖民机械族  (2)

当  本主体.电量   小于   30%
，  本主体         充电   1%

（属性列表里没有饱食度，也没有着装 —— 机械族没有它们。
  而且**没有一句"他是机械族"**：绑定已经说了。）
```

```
主体绑定：囚犯  (3)          ← 整个管制界面那一列都能用

当  本主体.活动区     等于   不受限
且  本主体.医疗级别   等于   不用药
，  本主体            改活动区    牢房
且  本主体            改医疗级别  草药或更差

（这两句玩家自己在管制界面也能点——规则的价值是**及时**：
  他一被放出来就自己回牢房，不用等你想起来。全部玩家级，不需要开发者权限。）
```

```
主体绑定：自由殖民者         ← 药物政策读得到"现场有哪些政策"

当  本主体.药物政策   等于   战斗兴奋剂
，  本主体            改敌对反应  逃跑
```

```
主体绑定：囚犯  (3)

当  本主体.心情   小于   20%
，  本图          写日志  有囚犯快崩溃了
```

```
主体绑定：（不绑定）

当  本图.囚犯数   小于   2
且  本图.奴隶数   等于   0
，  本图          写日志  人手不足
```

```
主体绑定：（不绑定）
（需要「允许开发者级操作」—— 见 §5）

当  本图.降雨强度   小于   0.1
，  本图             变成   下雨

当  本图.地图财富   小于   5000
，  本图             触发事件  袭击
且  本图             发信件    大威胁

当  本图             处于   日蚀
，  本图             发信件  负面事件
```

```
主体绑定：殖民机械族          ← **关键是这一行，不是「全部机械族」这个根**

当  本主体.电量      小于   50%
且  本主体.在露天     为真
且  本图.是白天       为真
，  本主体            充电   1%

（每个机械族各跑一遍：a 在自己电量低于 50% 且自己露天时才充，
  b 也一样——**"每个各判一次"是主体绑定干的活**）
```

**对比一下写不出来的那种写法**：

```
主体绑定：（不绑定）

当  全部机械族.电量   小于   50%     ← ❌ 编辑器不给你属性列表
```

`全部机械族` 是一整组东西，它**读不了属性**——"这一组东西的电量"本来就不是一个值。
它只能 `.数量`（几个机械族）或 `.第一个`。要点进去看编辑器会怎么说（上面那一段解释
就是它当场给出来的）。

### 量词补上之后，这一句终于写得出来了

```
主体绑定：点名一个人 → 小明

当  小明.着装.全都满足（当前元素.耐久 大于 50%）   为真
，  小明                                            脱下   小明.着装.第一个

（"我的衣服每一件都完好" —— 原来是写不出来的：
  `.第一个.耐久 大于 50%` 只问"有一件好的"，
  `[耐久 大于 50%].数量 等于 0` 只能表达"一件好的都没有"。
  要表达"每一件都好"需要否定，而语言里没有否定。）
```

### 写不出（语法级缺口）

| 想说的话 | 为什么写不出 |
|---|---|
| 帽子的耐久总和 / 最大 / 最小 | 缺 lift 型归约（把属性映射到集合上再归约）。量词产出的是一句话，它产出的是数 |
| 殖民者数 大于 敌人数 | 宾语只能是字面量，不能是另一条实体路径（`RuleOperand` 支持，但数值谓词的宾语只收字面值） |
| 他穿的是不是某件**具体**装备 | `物品.物品类型` 比的是类型不是实例 |
| 「且」和「或」混在同一层 | 现在只能靠嵌套组（`+ 或` 会建一个缩进子块） |
| 距离 / 方向相关的判断 | 没有"距离""朝向"这类属性 |
| 技能 / 特质 / 关系 | 没有对应属性（也没有带参数的属性，比如"某技能的等级"） |
| 「某监狱里的囚犯」这种二次筛选 | 主体绑定只有整图的六类，不能按房间/区域框定 |
| **改作息（把 14 点改成工作）** | 作息是 24 格的一张表，规则里没有"格子"这个概念。读当前这一格可以（`当前作息`），写不行——**说不清"改哪一格"的动词不如不给** |
| 「区分访客里的商队和难民」 | 身份判断被有意移出了规则语言（§0）。窄到需要它的那类绑定还没有 |

---

## 8. 加新词表

三种方式：

1. **改 RuleCore**：在 `Model/RuleVocabularyCatalog.cs` 里加一行（`AddXxx`）
2. **别的 mod 注册**：用 `RuleCore.Api.RuleCoreApi`，见 `Docs/扩展注册API.md`
3. **加一个能力位**：如果新属性只对某一类宿主有意义，先给 `RuleCapability` 加一位
   （`Core/RuleSemantics.cs`），在 `RulePawnFacts.OfPawn` 里给出判据，
   再在三份语言文件里补 `RuleCore.Cap.<名字>`

加一行 = 加一种玩法。**不需要加新的条件类型或动作类型**——
「室外温度大于10」不是一种类型，它是 `属性 × 比较符 × 阈值` 的组合。

**加身份类属性之前先读 §0**：如果新绑定仍然窄到决定了身份，那身份就不该进规则语言。
